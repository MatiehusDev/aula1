#include <stdio.h>

int fatorial(int a);

int main(){
    int num = 0;

    printf("\nDigite um numero para calcular o fatorial: ");
    scanf("%d%*c", &num);
    printf("%d! = %d", num, fatorial(num));

    return 0;
}

int fatorial(int a){
    if(n == 0){
        return 1;
    }
    else{
        return n-1 
    }
}